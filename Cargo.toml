[package]
name = "egui_picker"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = { version = "0.35.0", default-features = false, features = ["glow", "default_fonts"] }
rfd = "0.17.2"
serde_json = "1.0.151"

[build-dependencies]
winresource = "0.1"

[profile.release]
strip = true
lto = "thin"
opt-level = "s"
